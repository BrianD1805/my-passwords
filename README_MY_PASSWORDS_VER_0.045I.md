# My Passwords Ver-0.045I — Popup Top Edge Final Correction

This corrective patch removes the unintended decorative green overlay that ran down the popup's right side.

All popups now use the same border construction as the vault login card:

- a normal faint 1px outer border;
- one 5px top border following both rounded top corners;
- green used for the top edge;
- no green right-side border, gradient, fade, mask or overlay.

No popup sizing, sticky header/footer, scrolling, vault, sync, subscription or encryption logic was changed.
