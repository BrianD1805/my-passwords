# My Passwords Ver-0.042J

## Mobile vault and login UX

- Uses one consistent light-blue page background on mobile for the vault login and unlocked vault.
- Prevents the mobile pull-down refresh gesture from reloading and locking the vault.
- Adds a compact refresh button beside Vault Safety status on mobile.
- Refresh checks the latest secure cloud copy and backs up newer local changes safely.
- Restores the vault-login secure-device key and Unlock Local Vault button to the original neutral blue styling.
- Applies Ubuntu globally across the landing page, private vault and single-site Admin.

## Data and database

- No Supabase SQL changes.
- No encrypted vault, tenant, billing, document or Emergency Access schema changes.
