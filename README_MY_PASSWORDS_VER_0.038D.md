# My Passwords Ver-0.038D — Secure device unlock input layout fix

## What changed
- Restored the master password input to the same full width as the Unlock Local Vault button.
- Moved the secure-device key icon inside the password input on the right, beside the show/hide eye icon.
- Kept the key icon plain against the input, without a separate outside button block.
- Fixed the forced password reminder so the mobile keyboard does not open automatically behind the reminder popup.
- The keyboard now opens only after the user taps Enter master password.
- Improved the Security note padding and wording.

## Database
No Supabase SQL changes required.
